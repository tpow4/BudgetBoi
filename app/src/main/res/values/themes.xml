<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.BudgetBoi" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="windowActionModeOverlay">true</item>
    </style>

    <style name="Theme.BudgetBoi.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.BudgetBoi.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.BudgetBoi.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>